import type { NextPage } from 'next';
import { AdminPage } from '@/components/AdminPage';  // Changed to named import

const Admin: NextPage = () => {
  return <AdminPage />;
};

export default Admin;
